# Open Questions

## spec-review-consensus - 2026-03-04

- [ ] 폴더 드롭 시 내부 파일 개별 이벤트를 발생시킬지 여부 — v0.2에서 재논의 필요. 대용량 폴더(1000+파일) 시 성능 영향
- [ ] WebSocket 재연결 정책 — OpenClaw가 끊겼다 다시 연결될 때 놓친 이벤트를 어떻게 보상할지 (polling fallback? 이벤트 버퍼?)
- [ ] `POST /runs/:id/retry` 재실행 시 원본 파일이 이미 이동된 경우 처리 방안 — 워크플로 재시도의 멱등성 보장 문제
- [ ] 다중 파일 드롭 시 하나의 EVENT_INGESTED에 files[] 배열로 묶을지, 파일당 개별 이벤트로 할지 — 현재 SPEC은 개별 이벤트로 제안했으나 대량 드롭 시 이벤트 폭주 가능
- [ ] Draction 포트 번호 결정 방식 — 고정 포트 vs 동적 할당 + 디스커버리 파일(state.json)
- [ ] sha256 해시 계산 시점 — 드롭 즉시(UX 지연 가능) vs 백그라운드 비동기(중복 감지 지연 가능)
- [ ] Undo 10초 타이머의 시작 기준 — 파일 이동 완료 시점 vs 드롭 감지 시점 (대용량 파일에서 차이 발생)
- [ ] 기술 스택 ADR 작성 담당자 및 일정 — 스택 결정 전까지 구현 시작 불가
